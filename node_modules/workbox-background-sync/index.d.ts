import { BackgroundSyncPlugin } from 'workbox-background-sync/BackgroundSyncPlugin';
import { Queue, QueueOptions } from 'workbox-background-sync/Queue';
import { QueueStore } from 'workbox-background-sync/QueueStore';
import { StorableRequest } from 'workbox-background-sync/StorableRequest';
import 'workbox-background-sync/_version';
interface SyncManager {
    getTags(): Promise<string[]>;
    register(tag: string): Promise<void>;
}
declare global {
    interface ServiceWorkerRegistration {
        readonly sync: SyncManager;
    }
    interface SyncEvent extends ExtendableEvent {
        readonly lastChance: boolean;
        readonly tag: string;
    }
    interface ServiceWorkerGlobalScopeEventMap {
        sync: SyncEvent;
    }
}
/**
 * @module workbox-background-sync
 */
export { BackgroundSyncPlugin, Queue, QueueOptions, QueueStore, StorableRequest };
