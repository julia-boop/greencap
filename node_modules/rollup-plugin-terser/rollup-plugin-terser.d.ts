import { Plugin } from "rollup-plugin-terser/node_modules/rollup";
import { MinifyOptions } from "rollup-plugin-terser/node_modules/terser";

export interface Options extends Omit<MinifyOptions, "sourceMap"> {
  /**
   * Amount of workers to spawn. Defaults to the number of CPUs minus 1.
   */
  numWorkers?: number;
}

export declare function terser(options?: Options): Plugin;
